# simple-docker-example
An implementation of Docker on a placeholder web application.
Docker is a powerful, open-source tool used to deliver software compatible with any relevant machine. It bundles an application's code, runtime, system tools, and libraries together in a lightweight package to resolve the "but it works on my machine!" issue.
